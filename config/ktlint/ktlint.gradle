apply plugin: "org.jlleitschuh.gradle.ktlint"

android {
    lintOptions {
        abortOnError true
    }
}

ktlint {
    android.set(true)
    outputColorName.set("RED")
    outputToConsole.set(true)
}
